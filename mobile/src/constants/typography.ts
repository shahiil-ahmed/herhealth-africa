/**
 * HerHealth Typography
 * Mirrors the web app's font usage (Fraunces for headings, Jost for body)
 */
export const Typography = {
  // Font families
  serif: 'serif',       // Fraunces equivalent (system serif)
  sans: 'System',       // Jost equivalent (system sans)

  // Font sizes
  xs: 10,
  sm: 12,
  base: 14,
  md: 15,
  lg: 17,
  xl: 20,
  '2xl': 24,
  '3xl': 28,
  '4xl': 32,
  '5xl': 40,

  // Line heights
  tight: 1.2,
  normal: 1.5,
  relaxed: 1.6,

  // Letter spacing
  wide: 1.2,
  wider: 2,
};

export default Typography;
